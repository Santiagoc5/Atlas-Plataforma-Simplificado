/**
 * Indicador visual de carga animado.
 * Utilizado para brindar feedback al usuario durante peticiones asíncronas a la API.
 */
const Spinner = () => <span className="spinner" />;

export default Spinner;